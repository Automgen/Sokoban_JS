class Niveau {

    constructor(num) {
        this.num = num;
        this.grid = [];
        for (let i = 0; i < 20; i++) {
            let ligne = [];
            for (let j = 0; j < 20; j++) {
                //TODO créer un élément en se basant sur un fichier csv représentant un niveau
            }
        }
    }

}